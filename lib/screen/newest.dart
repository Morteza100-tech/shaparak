import 'package:flutter/material.dart';

class Newest extends StatelessWidget {
  static String title = 'Newest';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Newest!'),
      ),
    );
  }
}
